"""
agents.tests — Tests propios del sistema de agentes.

No están en `testpaths` de pyproject.toml (esa carpeta pertenece al proyecto
generado y esta plantilla no debía modificarla) — ejecútalos explícitamente:

    uv run pytest agents/tests/ -v

Si quieres que `make test` los incluya también, añade "agents/tests" a
`testpaths` en pyproject.toml tú mismo — es una línea, pero es una decisión
tuya sobre tu proyecto, no algo que este template deba imponer.
"""
